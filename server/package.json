{
  "name": "quietway-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Postgres-backed API for the QuietWay frontend, implementing the endpoints frontend/src/services/api.js calls.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "seed": "node scripts/seed.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "pg-mem": "^2.9.1",
    "supertest": "^7.2.2"
  }
}
